scmVersion {
 	tag {
		prefix = 'v'
		versionSeparator = '_'
	}
//	localOnly = false
  	checks { 
        uncommittedChanges = false
        aheadOfRemote = true
        snapshotDependencies = false
    } 
	repository {
    	pushTagsOnly = true
    }
}