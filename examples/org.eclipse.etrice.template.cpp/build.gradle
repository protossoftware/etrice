apply plugin: 'cpp'
apply from: "$rootDir/gradle/etTest.gradle"

dependencies {
	modelpath_cpp files('model')
}

def models = [
	'model/TemplateMapping.etmap',
	'model/TemplateModel.room'
]

createBuild('cpp', 'template', models)