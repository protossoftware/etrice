/**
 * @author generated by eTrice
 *
 * Source File of ActorClass ATcpServer
 * 
 */

#include "ATcpServer.h"

#include "modelbase/etActor.h"
#include "debugging/etLogger.h"
#include "debugging/etMSCLogger.h"
#include "etUnit/etUnit.h"
#include "platform/etMemory.h"

#include "room/basic/service/tcp/PTcpControl.h"
#include "room/basic/service/tcp/PTcpPayload.h"

/*--------------------- begin user code ---------------------*/
/* user code 3 */
/*--------------------- end user code ---------------------*/

/* interface item IDs */
enum interface_items {
	IFITEM_ControlPort = 1,
	IFITEM_PayloadPort = 2
};

/* state IDs */
enum state_ids {
	NO_STATE = 0,
	STATE_TOP = 1,
	STATE_closed = 2,
	STATE_opened = 3,
	STATE_error = 4,
	STATE_MAX = 5
};

/* transition chains */
enum chain_ids {
	CHAIN_TRANS_INITIAL_TO__closed = 1,
	CHAIN_TRANS_tr0_FROM_closed_TO_cp0_BY_openControlPort = 2,
	CHAIN_TRANS_tr1_FROM_opened_TO_closed_BY_closeControlPort = 3,
	CHAIN_TRANS_tr3_FROM_opened_TO_opened_BY_sendPayloadPort_tr3 = 4
};

/* triggers */
enum triggers {
	POLLING = 0,
	TRIG_PayloadPort__send = IFITEM_PayloadPort + EVT_SHIFT*PTcpPayload_IN_send,
	TRIG_ControlPort__close = IFITEM_ControlPort + EVT_SHIFT*PTcpControl_IN_close,
	TRIG_ControlPort__open = IFITEM_ControlPort + EVT_SHIFT*PTcpControl_IN_open
};


static void setState(ATcpServer* self, int new_state) {
	self->state = new_state;
}

static int getState(ATcpServer* self) {
	return self->state;
}

/* Entry and Exit Codes */

/* Action Codes */
static void action_TRANS_tr0_FROM_closed_TO_cp0_BY_openControlPort(ATcpServer* self, const InterfaceItemBase* ifitem, DTcpControl* data) {
	self->lastError /* ORIG: lastError */=0;
	/* start accept thread */
	if (etStartListening(self->server /* ORIG: server */, data->TcpPort)!=ETSOCKET_OK) self->lastError /* ORIG: lastError */ = 1;
}
static void action_TRANS_tr1_FROM_opened_TO_closed_BY_closeControlPort(ATcpServer* self, const InterfaceItemBase* ifitem) {
	/* close accept thread */
	etCloseAllServerSockets(self->server /* ORIG: server */);
	etStopSocketServer(self->server /* ORIG: server */);
}
static void action_TRANS_tr2_FROM_cp0_TO_opened(ATcpServer* self, const InterfaceItemBase* ifitem, DTcpControl* data) {
	PTcpControlPort_established(&self->constData->ControlPort) /* ORIG: ControlPort.established() */;
}
static void action_TRANS_socketError_FROM_cp0_TO_error_COND_socketError(ATcpServer* self, const InterfaceItemBase* ifitem, DTcpControl* data) {
	PTcpControlPort_error(&self->constData->ControlPort) /* ORIG: ControlPort.error() */;
	/* handle not connected */
}
static void action_TRANS_tr3_FROM_opened_TO_opened_BY_sendPayloadPort_tr3(ATcpServer* self, const InterfaceItemBase* ifitem, DTcpPayload* data) {
	/* send payload to connection */
	etWriteServerSocket(self->server /* ORIG: server */, data->connectionId, data->length, data->data);
}

/**
 * calls exit codes while exiting from the current state to one of its
 * parent states while remembering the history
 * @param current - the current state
 * @param to - the final parent state
 */
static void exitTo(ATcpServer* self, etInt16 current, etInt16 to) {
	while (current!=to) {
		switch (current) {
			case STATE_closed:
				self->history[STATE_TOP] = STATE_closed;
				current = STATE_TOP;
				break;
			case STATE_opened:
				self->history[STATE_TOP] = STATE_opened;
				current = STATE_TOP;
				break;
			case STATE_error:
				self->history[STATE_TOP] = STATE_error;
				current = STATE_TOP;
				break;
			default:
				/* should not occur */
				break;
		}
	}
}

/**
 * calls action, entry and exit codes along a transition chain. The generic data are cast to typed data
 * matching the trigger of this chain. The ID of the final state is returned
 * @param chain - the chain ID
 * @param generic_data - the generic data pointer
 * @return the +/- ID of the final state either with a positive sign, that indicates to execute the state's entry code, or a negative sign vice versa
 */
static etInt16 executeTransitionChain(ATcpServer* self, int chain, const InterfaceItemBase* ifitem, void* generic_data) {
	switch (chain) {
		case CHAIN_TRANS_INITIAL_TO__closed:
		{
			return STATE_closed;
		}
		case CHAIN_TRANS_tr0_FROM_closed_TO_cp0_BY_openControlPort:
		{
			DTcpControl* data = ((DTcpControl*) generic_data);
			action_TRANS_tr0_FROM_closed_TO_cp0_BY_openControlPort(self, ifitem, data);
			if (self->lastError /* ORIG: lastError */!=0) {
			action_TRANS_socketError_FROM_cp0_TO_error_COND_socketError(self, ifitem, data);
			return STATE_error;}
			else {
			action_TRANS_tr2_FROM_cp0_TO_opened(self, ifitem, data);
			return STATE_opened;}
		}
		case CHAIN_TRANS_tr1_FROM_opened_TO_closed_BY_closeControlPort:
		{
			action_TRANS_tr1_FROM_opened_TO_closed_BY_closeControlPort(self, ifitem);
			return STATE_closed;
		}
		case CHAIN_TRANS_tr3_FROM_opened_TO_opened_BY_sendPayloadPort_tr3:
		{
			DTcpPayload* data = ((DTcpPayload*) generic_data);
			action_TRANS_tr3_FROM_opened_TO_opened_BY_sendPayloadPort_tr3(self, ifitem, data);
			return STATE_opened;
		}
			default:
				/* should not occur */
				break;
	}
	return NO_STATE;
}

/**
 * calls entry codes while entering a state's history. The ID of the final leaf state is returned
 * @param state - the state which is entered
 * @return - the ID of the final leaf state
 */
static etInt16 enterHistory(ATcpServer* self, etInt16 state) {
	boolean skip_entry = FALSE;
	if (state >= STATE_MAX) {
		state = state - STATE_MAX;
		skip_entry = TRUE;
	}
	while (TRUE) {
		switch (state) {
			case STATE_closed:
				/* in leaf state: return state id */
				return STATE_closed;
			case STATE_opened:
				/* in leaf state: return state id */
				return STATE_opened;
			case STATE_error:
				/* in leaf state: return state id */
				return STATE_error;
			case STATE_TOP:
				state = self->history[STATE_TOP];
				break;
			default:
				/* should not occur */
				break;
		}
		skip_entry = FALSE;
	}
	/* return NO_STATE; // required by CDT but detected as unreachable by JDT because of while (true) */
}

static void ATcpServer_executeInitTransition(ATcpServer* self) {
	int chain = CHAIN_TRANS_INITIAL_TO__closed;
	etInt16 next = executeTransitionChain(self, chain, NULL, NULL);
	next = enterHistory(self, next);
	setState(self, next);
}

/* receiveEvent contains the main implementation of the FSM */
static void ATcpServer_receiveEvent(ATcpServer* self, InterfaceItemBase* ifitem, int evt, void* generic_data) {
	int trigger = ifitem->localId + EVT_SHIFT*evt;
	int chain = NOT_CAUGHT;
	etInt16 catching_state = NO_STATE;
	
	if (!handleSystemEvent(ifitem, evt, generic_data)) {
		switch (getState(self)) {
			case STATE_closed:
				switch(trigger) {
						case TRIG_ControlPort__open:
							{
								chain = CHAIN_TRANS_tr0_FROM_closed_TO_cp0_BY_openControlPort;
								catching_state = STATE_TOP;
							}
						break;
						default:
							/* should not occur */
							break;
				}
				break;
			case STATE_opened:
				switch(trigger) {
						case TRIG_ControlPort__close:
							{
								chain = CHAIN_TRANS_tr1_FROM_opened_TO_closed_BY_closeControlPort;
								catching_state = STATE_TOP;
							}
						break;
						case TRIG_PayloadPort__send:
							{
								chain = CHAIN_TRANS_tr3_FROM_opened_TO_opened_BY_sendPayloadPort_tr3;
								catching_state = STATE_TOP;
							}
						break;
						default:
							/* should not occur */
							break;
				}
				break;
			case STATE_error:
				break;
			default:
				/* should not occur */
				break;
		}
	}
	if (chain != NOT_CAUGHT) {
		exitTo(self, getState(self), catching_state);
		{
			etInt16 next = executeTransitionChain(self, chain, ifitem, generic_data);
			next = enterHistory(self, next);
			setState(self, next);
		}
	}
}

void ATcpServer_init(ATcpServer* self){
	ET_MSC_LOGGER_SYNC_ENTRY("ATcpServer", "init")
	self->state = STATE_TOP;
	{
		int i;
		for (i=0; i<ATCPSERVER_HISTORY_SIZE; ++i)
			self->history[i] = NO_STATE;
	}
	ATcpServer_executeInitTransition(self);
	ET_MSC_LOGGER_SYNC_EXIT
}


void ATcpServer_receiveMessage(void* self, void* ifitem, const etMessage* msg){
	ET_MSC_LOGGER_SYNC_ENTRY("ATcpServer", "_receiveMessage")
	
	ATcpServer_receiveEvent(self, (etPort*)ifitem, msg->evtID, (void*)(((char*)msg)+MEM_CEIL(sizeof(etMessage))));
	
	ET_MSC_LOGGER_SYNC_EXIT
}


/*--------------------- operations ---------------------*/
void ATcpServer_ctor(ATcpServer* self) {
	etInitSockets();
	self->server /* ORIG: server */ = etCreateSocketServerData();
}
void ATcpServer_dtor(ATcpServer* self) {
	etCleanupSockets();
	etFreeSocketServerData(self->server /* ORIG: server */);
}
void ATcpServer_stopUser(ATcpServer* self) {
	/* stop user: close socket */
}

